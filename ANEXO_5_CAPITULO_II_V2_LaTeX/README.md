# Proyecto LaTeX convertido desde Word

Archivo principal: `main.tex`

Compilación recomendada:

```bash
xelatex main.tex
xelatex main.tex
```

Se usa XeLaTeX para mantener tildes, caracteres Unicode y una tipografía compatible. Las imágenes necesarias están en la carpeta `media/`.
